export enum RouteKey {
  // Main pages
  HOME = "Home",
  LOGIN = "Login",
  SIGNUP = "Signup",
  USER_DETAIL = "UserDetail",
}

export enum SearchParams {
  Query = "q",
  View = "view",

  // Auth
  Auth = "auth",
}
